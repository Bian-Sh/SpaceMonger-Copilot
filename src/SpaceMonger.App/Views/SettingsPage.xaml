<UserControl x:Class="SpaceMonger.App.Views.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:loc="clr-namespace:SpaceMonger.App.Localization"
             xmlns:vm="clr-namespace:SpaceMonger.App.ViewModels"
             xmlns:enums="clr-namespace:SpaceMonger.Core.Enums;assembly=SpaceMonger.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel, IsDesignTimeCreatable=False}">
    <Grid Background="#F4F6FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#E9EDF5" Padding="12">
            <DockPanel>
                <Button DockPanel.Dock="Top"
                        Content="{loc:Loc SettingsBackToAppButton}"
                        HorizontalAlignment="Left"
                        Padding="8,4"
                        Margin="0,0,0,16"
                        Click="BackButton_Click" />
                <TextBox DockPanel.Dock="Top"
                         Margin="0,0,0,20"
                         Padding="8,5"
                         IsEnabled="False"
                         Text="{loc:Loc SettingsSearchPlaceholder}" />
                <StackPanel>
                    <TextBlock Text="{loc:Loc SettingsPersonalGroup}" Foreground="#77808C" Margin="4,0,0,8" />
                    <Border Background="#DDE4EF" CornerRadius="8" Padding="12,8" Margin="0,0,0,6">
                        <TextBlock Text="{loc:Loc SettingsGeneralNav}" FontWeight="SemiBold" />
                    </Border>
                    <TextBlock Text="AI" Foreground="#77808C" Margin="4,18,0,8" />
                    <Border Background="#DDE4EF" CornerRadius="8" Padding="12,8" Margin="0,0,0,6">
                        <TextBlock Text="{loc:Loc SettingsModelInterfaceNav}" FontWeight="SemiBold" />
                    </Border>
                    <Border Background="Transparent" CornerRadius="8" Padding="12,8" Margin="0,0,0,6">
                        <TextBlock Text="{loc:Loc SettingsCleanupPolicyNav}" />
                    </Border>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Margin="0" Background="White" CornerRadius="14,0,0,14" Padding="48,36">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="{loc:Loc SaveButton}" Width="96" Height="32" IsDefault="True" Click="SaveButton_Click" />
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel MaxWidth="760">
                        <TextBlock Text="{loc:Loc SettingsTitle}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,28" />

                        <TextBlock Text="{loc:Loc SettingsAiApiSection}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                        <Border BorderBrush="#E5E5E5" BorderThickness="1" CornerRadius="10" Margin="0,0,0,24">
                            <StackPanel>
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{loc:Loc AnthropicApiKeyHeader}" FontWeight="SemiBold" />
                                        <TextBlock Text="{loc:Loc SettingsApiKeyEncryptedHint}" Foreground="#666" FontSize="12" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1">
                                        <TextBox Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" Padding="6" ToolTip="{loc:Loc ApiKeyToolTip}" />
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Button Content="{loc:Loc ValidateButton}" Command="{Binding ValidateCommand}" Width="90" />
                                            <TextBlock Margin="10,2,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ValidationState}" Value="{x:Static vm:ValidationState.Validating}">
                                                                <Setter Property="Text" Value="Validating..." />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ValidationState}" Value="{x:Static vm:ValidationState.Valid}">
                                                                <Setter Property="Text" Value="✔ Valid" />
                                                                <Setter Property="Foreground" Value="Green" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ValidationState}" Value="{x:Static vm:ValidationState.Invalid}">
                                                                <Setter Property="Text" Value="✘ Invalid" />
                                                                <Setter Property="Foreground" Value="Red" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Text="{Binding ValidationMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,4,0,0" />
                                    </StackPanel>
                                </Grid>
                                <Separator />
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{loc:Loc AnthropicBaseUrlHeader}" FontWeight="SemiBold" />
                                        <TextBlock Text="{loc:Loc SettingsBaseUrlExample}" Foreground="#666" FontSize="12" />
                                    </StackPanel>
                                    <TextBox Grid.Column="1" Text="{Binding AnthropicBaseUrl, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{loc:Loc SettingsModelsSection}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                        <Border BorderBrush="#E5E5E5" BorderThickness="1" CornerRadius="10" Margin="0,0,0,24">
                            <StackPanel>
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{loc:Loc SettingsAnalysisModelLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text="{loc:Loc SettingsAnalysisModelHint}" Foreground="#666" FontSize="12" />
                                    </StackPanel>
                                    <TextBox Grid.Column="1" Text="{Binding AnalysisModelName, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                                </Grid>
                                <Separator />
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{loc:Loc SettingsChatModelLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text="{loc:Loc SettingsChatModelHint}" Foreground="#666" FontSize="12" />
                                    </StackPanel>
                                    <TextBox Grid.Column="1" Text="{Binding ChatModelName, UpdateSourceTrigger=PropertyChanged}" Padding="6" />
                                </Grid>
                                <Separator />
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{loc:Loc SettingsThinkingLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text="{loc:Loc SettingsThinkingHint}" Foreground="#666" FontSize="12" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" IsChecked="{Binding EnableThinking}" VerticalAlignment="Center" Content="{loc:Loc SettingsAllowThinkingOption}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{loc:Loc SettingsGeneralNav}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12" />
                        <Border BorderBrush="#E5E5E5" BorderThickness="1" CornerRadius="10">
                            <StackPanel>
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{loc:Loc SettingsLanguageLabel}" FontWeight="SemiBold" />
                                        <TextBlock Text="{loc:Loc SettingsLanguageHint}" Foreground="#666" FontSize="12" />
                                    </StackPanel>
                                    <ComboBox Grid.Column="1" SelectedValue="{Binding Language}" SelectedValuePath="Tag" Padding="4">
                                        <ComboBoxItem Tag="zh-CN" Content="{loc:Loc SettingsLanguageSimplifiedChinese}" />
                                        <ComboBoxItem Tag="en" Content="{loc:Loc SettingsLanguageEnglish}" />
                                        <ComboBoxItem Tag="auto" Content="{loc:Loc SettingsLanguageAuto}" />
                                    </ComboBox>
                                </Grid>
                                <Separator />
                                <Grid Margin="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{loc:Loc SettingsDefaultCleanupModeLabel}" FontWeight="SemiBold" />
                                    <ComboBox Grid.Column="1" SelectedItem="{Binding SelectedDeletionMode}" Padding="4">
                                        <ComboBox.Items>
                                            <enums:DeletionMode>MoveToRecycleBin</enums:DeletionMode>
                                            <enums:DeletionMode>PermanentDelete</enums:DeletionMode>
                                        </ComboBox.Items>
                                    </ComboBox>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
