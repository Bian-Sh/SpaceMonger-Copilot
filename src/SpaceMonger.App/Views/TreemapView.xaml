<UserControl x:Class="SpaceMonger.App.Views.TreemapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:SpaceMonger.App.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Row 0: Breadcrumb bar -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Background="#F0F0F0">
            <Button x:Name="UpButton"
                    Content="&#x2190; Up"
                    Padding="6,2"
                    Margin="4,0"
                    IsEnabled="False"
                    Click="UpButton_Click"
                    ToolTip="Navigate up one level" />
            <StackPanel x:Name="BreadcrumbPanel"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="4,0" />
        </StackPanel>

        <!-- Row 1: TreemapControl -->
        <controls:TreemapControl Grid.Row="1"
                                 x:Name="Treemap"
                                 NodeClicked="Treemap_NodeClicked"
                                 NodeHovered="Treemap_NodeHovered"
                                 NodeRightClicked="Treemap_NodeRightClicked"
                                 SizeChanged="Treemap_SizeChanged" />

        <!-- Row 2: Color legend -->
        <Border Grid.Row="2"
                Background="#F0F0F0"
                BorderBrush="#D0D0D0"
                BorderThickness="0,1,0,0"
                Padding="6,4">
            <WrapPanel x:Name="LegendPanel"
                       Orientation="Horizontal" />
        </Border>
    </Grid>
</UserControl>
