<UserControl x:Class="SpaceMonger.App.Controls.CodeBlockControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="500">

    <Border Background="#1E1E1E"
            CornerRadius="4"
            Padding="0"
            BorderBrush="#3E3E3E"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Row 0: Header bar with language label and Copy button -->
            <Border Grid.Row="0"
                    Background="#2D2D2D"
                    CornerRadius="4,4,0,0"
                    Padding="10,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding Language, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Foreground="#888888"
                               FontSize="11"
                               VerticalAlignment="Center" />

                    <Button Grid.Column="1"
                            x:Name="CopyButton"
                            Content="Copy"
                            Click="CopyButton_Click"
                            Padding="8,2"
                            FontSize="11"
                            Background="#3E3E3E"
                            Foreground="#CCCCCC"
                            BorderBrush="#555555"
                            BorderThickness="1"
                            Cursor="Hand"
                            VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Row 1: Code content -->
            <TextBlock Grid.Row="1"
                       Text="{Binding CodeText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Foreground="#D4D4D4"
                       FontFamily="Consolas"
                       FontSize="13"
                       TextWrapping="Wrap"
                       Padding="12,10"
                       VerticalAlignment="Top" />
        </Grid>
    </Border>
</UserControl>
