<UserControl x:Class="SpaceMonger.App.Controls.ColorPickerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Width="260" Height="200">

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- SV Picker (Saturation-Value) -->
        <Border Grid.Row="0" CornerRadius="6" ClipToBounds="True"
                BorderBrush="{DynamicResource VP.BorderLightBrush}"
                BorderThickness="0.5" Margin="0,0,0,8">
            <Canvas x:Name="SVCanvas"
                    Background="Transparent"
                    MouseLeftButtonDown="SVCanvas_MouseLeftButtonDown"
                    MouseMove="SVCanvas_MouseMove"
                    MouseLeftButtonUp="SVCanvas_MouseLeftButtonUp">
                <!-- White-to-color horizontal gradient -->
                <Rectangle x:Name="SVRect" Width="260" Height="140">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="White" Offset="0" />
                            <GradientStop x:Name="HueColorStop" Color="Red" Offset="1" />
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <!-- Black vertical gradient overlay -->
                <Rectangle Width="260" Height="140">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="Transparent" Offset="0" />
                            <GradientStop Color="Black" Offset="1" />
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <!-- Selection circle -->
                <Ellipse x:Name="SVSelector" Width="14" Height="14"
                         Stroke="White" StrokeThickness="2"
                         Canvas.Left="0" Canvas.Top="0"
                         IsHitTestVisible="False">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="0" Opacity="0.6" />
                    </Ellipse.Effect>
                </Ellipse>
            </Canvas>
        </Border>

        <!-- Hue Slider -->
        <Border Grid.Row="1" CornerRadius="6" ClipToBounds="True" Height="20"
                BorderBrush="{DynamicResource VP.BorderLightBrush}"
                BorderThickness="0.5" Margin="0,0,0,8">
            <Canvas x:Name="HueCanvas"
                    Background="Transparent"
                    MouseLeftButtonDown="HueCanvas_MouseLeftButtonDown"
                    MouseMove="HueCanvas_MouseMove"
                    MouseLeftButtonUp="HueCanvas_MouseLeftButtonUp">
                <Rectangle Width="260" Height="20">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#FFFF0000" Offset="0.0" />
                            <GradientStop Color="#FFFFFF00" Offset="0.167" />
                            <GradientStop Color="#FF00FF00" Offset="0.333" />
                            <GradientStop Color="#FF00FFFF" Offset="0.5" />
                            <GradientStop Color="#FF0000FF" Offset="0.667" />
                            <GradientStop Color="#FFFF00FF" Offset="0.833" />
                            <GradientStop Color="#FFFF0000" Offset="1.0" />
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
                <!-- Hue selector line -->
                <Rectangle x:Name="HueSelector" Width="4" Height="20"
                           Stroke="White" StrokeThickness="1.5"
                           Canvas.Left="0" Canvas.Top="0"
                           IsHitTestVisible="False">
                    <Rectangle.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="0" Opacity="0.6" />
                    </Rectangle.Effect>
                </Rectangle>
            </Canvas>
        </Border>

        <!-- Color preview + Hex input -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" CornerRadius="4"
                    BorderBrush="{DynamicResource VP.BorderLightBrush}"
                    BorderThickness="0.5">
                <Border.Background>
                    <SolidColorBrush x:Name="PreviewBrush" Color="#FF2562A7" />
                </Border.Background>
            </Border>
            <TextBox x:Name="HexTextBox" Grid.Column="2"
                     Style="{DynamicResource VP.TextBox}"
                     FontSize="12" Padding="6,4"
                     Text="#FF2562A7"
                     KeyDown="HexTextBox_KeyDown"
                     LostFocus="HexTextBox_LostFocus" />
        </Grid>
    </Grid>
</UserControl>
