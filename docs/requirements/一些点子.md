`- [ ] 任务未完成`

`- [x] 任务已完成`

## 2026年6月27日

- [ ] 关闭时的提示中title不要带标点，消息中“将关闭“改为”将清空“ ，但是这个消息要做一个有意义的展示，如果用户没有扫描，没有分析，没有chat对话呢？所以是不是应该把提示做到有效有指向的呢？

- [ ] 导航栏的输入框的编辑模式实现记录/删除记录曾经粘贴过的路径的功能，删除按钮就是个叉叉，放在 dropdown item 右侧，item cell 内侧

- [ ] 导航栏的输入框保存的历史记录item，路径过长处理逻辑：item统一长度为 输入框长度，数据溢出到此长度限定（考虑删除按钮），被拦截的位置的几个可见字符变为 “...” 让用户知道数据被省略了，然后整个 cell 就会变成一个滑动条的感觉，但是这个滑动条不需要拖拽，用户鼠标偏离中心向左侧移动则推动路径左侧的显示直到完全显示，右侧同理。可以灵敏一些，比如向右移动60像素（这个值打个比方，请根据经验处理）就能完全展示路径，而不是鼠标移动到item正后方才能看清整个路径

- [ ] D:\AppData\Visual Studio\Projects\spacemonger-next\src\SpaceMonger.Core\Services\Copilot\AiSkillRouter.cs 这里看着像是硬编码，我们需要 agent first + app-guide 来解释自己，一般是用户自主@指定 skill。否则通过 app system prompt 决定使用使用哪个 skill，我们需要让 AI知道它有啥 skill 可以使用。

- [ ] 将免责声明中加入作者不会直到用户 AI 配置，密钥的描述。