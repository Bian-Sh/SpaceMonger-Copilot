# TODO：导航栏与 Treemap 后续修复项（2026-06-17）

## 背景

本轮对导航栏、Treemap 和面包屑下拉做过多次尝试，其中以下三项实现没有达到预期，已从本次提交中排除。后续需要重新设计方案后再实现，不应继续沿用已撤回的临时方案。

## 待办项

### 1. 文件夹选择后的导航联动

期望行为：

- 用户通过导航栏文件夹按钮选择文件夹后，地址栏应立即展示所选路径。
- 如果该路径包含在上一轮扫描结果中，Treemap 应跳转并展示该目录。
- 如果该路径不包含在上一轮扫描结果中，Treemap 应进入默认/需重新分析提示页。

当前问题：

- 已尝试通过 `SelectedPath` 变化统一调用 `NavigateToPathOrSelect()`，但行为仍不稳定，已撤回。
- 后续需要明确 `SelectedPath`、`CurrentRoot`、扫描根节点和地址栏显示路径的单一数据源，避免相互触发和状态分裂。

### 2. Treemap 文字清晰度与中文显示

期望行为：

- Treemap 内文字应清晰、锐利。
- 中文目录名/文件名不能显示为小方块。

当前问题：

- 已尝试切换 SkiaSharp 字体链和 subpixel/hinting 参数，但效果未确认达标，已撤回。
- 后续应优先验证 WPF/SkiaSharp 文字渲染管线、DPI 缩放、字体 fallback 和 `SKFont` / `SKPaint` 版本行为，再做小范围实现。

### 3. 面包屑 `>` 下拉菜单过长与交互

期望行为：

- 点击面包屑中的 `>` 时，目录过多的 dropdown 不应铺满全屏。
- 空间不足时应可滚动，行为应接近 Windows 11 资源管理器。
- 当前一个 dropdown 打开时点击另一个 `>`，应稳定切换到新的 dropdown，不应瞬间隐藏。
- 内容目录过多时不应明显卡顿。

当前问题：

- 已尝试给 `ContextMenu` 动态设置 `MaxHeight` 和滚动模板，但仍存在卡顿和切换时瞬间隐藏的问题，已撤回。
- 后续可能需要改为自定义 Popup/ListBox 虚拟化方案，而不是继续堆叠 `ContextMenu`/`MenuItem`。

## 注意事项

- 以上三项不属于本次提交内容；本次提交只保留其它已确认修复。
- 修改中文资源文件时必须用真实 UTF-8 内容检查，不能依据 PowerShell 或 diff 的显示乱码判断文件损坏。
